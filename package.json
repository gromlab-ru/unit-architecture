{
  "name": "unit-architecture",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm run build:skill && npm run docs:build",
    "build:skill": "node scripts/build-skill.mjs",
    "docs:dev": "vitepress dev site",
    "docs:build": "vitepress build site",
    "docs:preview": "vitepress preview site"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "vitepress": "1.6.4"
  }
}
